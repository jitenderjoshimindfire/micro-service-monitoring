apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-a
  namespace: monitoring-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-a
  template:
    metadata:
      labels:
        app: service-a
    spec:
      containers:
        - name: service-a
          image: your-dockerhub-username/service-a:latest
          ports:
            - containerPort: 3000
          env:
            - name: REDIS_HOST
              value: redis
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: monitoring-demo
spec:
  type: ClusterIP
  selector:
    app: service-a
  ports:
    - name: http
      port: 3000
      targetPort: 3000
