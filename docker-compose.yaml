version: "3.8"
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  service-a:
    build: ./services/service-a
    depends_on: [redis]
    environment:
      - REDIS_HOST=redis
    ports:
      - "3000:3000"

  service-b:
    build: ./services/service-b
    depends_on: [redis]
    environment:
      - REDIS_HOST=redis

  service-c:
    build: ./services/service-c
    depends_on: [redis]
    environment:
      - REDIS_HOST=redis
    ports:
      - "4000:4000"
